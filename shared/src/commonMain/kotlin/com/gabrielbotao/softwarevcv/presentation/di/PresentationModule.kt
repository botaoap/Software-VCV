package com.gabrielbotao.softwarevcv.presentation.di

import org.koin.dsl.module

/** Presentation-layer bindings: ViewModels (`viewModel { … }`). Populated per feature card (VCV-6…). */
val presentationModule = module { }
