package com.gabrielbotao.softwarevcv.domain

/*
 * Domain layer — pure common Kotlin, zero platform imports. Holds models, value types, repository
 * interfaces, and use cases (one per class, `operator fun invoke()`). Populated by VCV-5 (catalog).
 * See [[Content-Model]] and [[KMP-Clean-Architecture]] §2.
 */
