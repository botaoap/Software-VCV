package com.gabrielbotao.softwarevcv.data

/*
 * Data layer — repository implementations, remote (Ktor) + content (bundled JSON) behind the
 * `VcvContentApi` seam, DTOs + mappers, settings wrapper. DTOs never leave this layer. Populated by
 * VCV-5. See [[Content-Model]] and [[KMP-Clean-Architecture]] §2.
 */
